# Changesets

Every user-facing change needs a changeset. Run `pnpm changeset`, pick the packages you touched and the bump type, and describe the change for the changelog.

All `@needless-ui/*` packages are versioned together, so a release always ships matching versions of tokens, CSS and framework packages.
